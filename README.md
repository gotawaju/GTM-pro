# GTM Digest

米国のGTM（Go-To-Market）関連情報を、日本語の判定つきリンク集として届ける静的サイトです。

## 開発

```sh
npm install
npm run dev
```

## ビルド

```sh
npm run build
```

Cloudflare Pages では、ビルドコマンドを `npm run build`、出力ディレクトリを `dist` に設定します。
